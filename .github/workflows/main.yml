name: E2E on Chrome

on: [push]

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Cypress run
        uses: charleskwakye/cypressProject
        with:
          project: cypress/e2e
          browser: chrome
          # build: yarn build
          # start: yarn start
          #wait-on: http://localhost:3000
          #testFiles: cypress/e2e/example.cy.js
        